
[server]
host = "{{ parameters.SERVER_HOST }}"
port = {{ parameters.SERVER_PORT }}

[database]
host = "{{ parameters.DATABASE_HOST }}"
port = {{ parameters.DATABASE_PORT }}
database = "{{ parameters.DATABASE_NAME }}"
user = "{{ parameters.DATABASE_USER }}"
password = "{{ secrets.DATABASE_PASSWORD }}"

[urls]
ping = "{{ urls.URL_PING }}"
expenses = "{{ urls.URL_EXPENSES }}"
expense = "{{ urls.URL_EXPENSE }}"

[templates]
read_expenses = "{{ templates.TEMPLATE_READ_EXPENSES }}"
read_expense = "{{ templates.TEMPLATE_READ_EXPENSE }}"
