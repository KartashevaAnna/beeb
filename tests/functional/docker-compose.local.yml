services:
  beeb:
    build:
      context: ../../
      dockerfile: Dockerfile
      target: production
    entrypoint: >
      sh -c 'CONFIG_SECRETS_PATH=./tests/functional/test.secrets.toml CONFIG_PATH=config.toml CONFIG_RENDERER=jinja2 python3 -m pytest tests -p no:cacheprovider --cov=. --cov-report xml:/out/coverage.xml --junitxml /out/report.xml && coverage report -m -i'

